# cctop v4.0.0 開発ロードマップ - 実動作駆動開発（RDD）

**Document ID**: cctop-v4-development-roadmap  
**Date**: 2025-06-22  
**Created**: 2025-06-22 20:45  
**Author**: Inspector Agent  
**Status**: Active  
**Purpose**: cctop v4.0.0の段階的開発計画（データフロー重視・詳細計画必須）

## 概要

version-02での失敗（テスト成功率100%でも起動しない）から学んだ教訓を活かし、**実動作駆動開発（Running-Driven Development: RDD）**の方法論に基づいて、実動作を確認しながら段階的に機能を追加していく開発計画。

## 実動作駆動開発（RDD）の理念

### 従来のTDDの限界
- **テスト成功 ≠ 実動作保証**: version-02で652テスト全成功も実環境で起動せず
- **モックの罠**: モックオブジェクトが実装と乖離し、虚偽の安心感を生む
- **統合の盲点**: 個別コンポーネントは動作しても、統合時に破綻

### RDDの中核原則
1. **実行可能性最優先**: 「npm start」で即座に動作することが全ての前提
2. **日次動作確認**: 毎日必ず実環境で動作確認し、動かない変更は即座にロールバック
3. **段階的統合**: 新機能は必ず動作中のシステムに統合し、動作確認後にのみ次へ進む
4. **実ユーザー視点**: 開発者の都合ではなく、実際の使用者の体験を重視

## 開発原則（RDD準拠）

1. **動くものを維持する** - 各段階で必ず実環境動作を確認
2. **段階的な複雑性** - シンプルから始めて徐々に機能追加
3. **継続的な品質向上** - cacheとtestは全段階で改善
4. **実動作優先** - テストよりも「npm start」での動作確認を優先
5. **各Phaseは詳細計画を作成すること** - 実装前に必ず詳細計画書を作成
6. **データの流れを明示すること** - 各Phaseでデータフローを明確に定義

## Phase 1: 基本ファイル監視（未実装）

### 目標
chokidarを使用したリアルタイムファイル監視と組み込み初回スキャン機能

### データフロー
```
[chokidar] ---> [DB] ---> [console] ---> [ユーザー]
     |           |           |             |
ファイル変更   永続化保存   表示        操作・確認
イベント検出                           (MVP: 表示確認のみ)
```

### 要件
- chokidarによるファイル監視
- 初回スキャン機能
- CLI引数処理
- ファイル変更イベントをリアルタイムでコンソールに流す基本的な表示機能
- RDD方式でのテスト実装（実動作確認を最優先、テストは補助的役割）

### 詳細計画
**作成予定**: `ip009-phase1-basic-monitoring.md`

## Phase 2: 移動・リネーム検出

### 目標
ファイルの移動・リネーム操作の正確な検出とstream表示

### データフロー
```
[chokidar] ---> [DB] ---> [console] ---> [ユーザー]
     |           |           |             |
ファイル変更   永続化保存   表示        確認
移動・リネーム  move検出    (move検出・stream表示)
```

### 技術課題
- chokidarはmoveイベントを提供しない
- unlink → addのペアをタイミング判定で検出
- 誤検出の防止（inode比較、内容ハッシュ）
- 大量ファイル移動時の精度維持

### 必須要件
- 単純なmv操作の100%検出
- ディレクトリ間移動の正確な検出
- false positiveの最小化

### 詳細計画
**作成予定**: `ip010-phase2-move-rename-detection.md`

## Phase 3: 重複イベント統合・詳細表示

### 目標
同一ファイルの複数イベント統合とファイル詳細情報の表示

### データフロー
```
[chokidar] ---> [DB] ---> [console] ---> [ユーザー]
     |           ↑           |             |
ファイル変更    |        表示        操作・確認
イベント検出    |                      |
              program操作 ←-----------┘
              (重複統合・詳細表示切替)
```

### UI設計
- **Allモード**: リアルタイム全イベント表示
- **Uniqueモード**: ファイル別統合表示
- **Detailモード**: 選択ファイルの詳細履歴

### 詳細計画
**作成予定**: `ip011-phase3-event-aggregation-detail.md`

## Phase 4: フィルタリング機能

### 目標
イベントタイプ・ファイル名・パスによる柔軟なフィルタリング

### データフロー
```
[chokidar] ---> [DB] ---> [console] ---> [ユーザー]
     |           ↑           |             |
ファイル変更    |        表示        操作・確認
イベント検出    |                      |
              program操作 ←-----------┘
              (フィルタリング設定)
```

### フィルタ種類
- **イベントタイプ**: add/change/unlink/move の選択
- **ファイル名**: 正規表現による絞り込み
- **パス**: 特定ディレクトリのみ表示
- **時間範囲**: 指定期間のイベント
- **ファイルサイズ**: サイズ範囲での絞り込み

### 詳細計画
**作成予定**: `ip012-phase4-filtering-system.md`

## Phase 5: 統計・分析機能

### 目標
ファイル活動の統計分析と可視化

### データフロー
```
[chokidar] ---> [DB] ---> [console] ---> [ユーザー]
     |           ↑           |             |
ファイル変更    |        表示        操作・確認
イベント検出    |                      |
              program操作 ←-----------┘
              (統計・分析問い合わせ)
```

### 統計項目
- **ファイル活動**: 最も変更の多いファイルTop10
- **時間分布**: 時間帯別・曜日別の活動パターン
- **イベント分布**: add/change/unlink/moveの比率
- **サイズ分析**: ファイルサイズ別の活動統計
- **拡張子分析**: ファイルタイプ別の活動状況

### 詳細計画
**作成予定**: `ip013-phase5-statistics-analysis.md`

## 継続的強化項目

### キャッシュシステム（全Phase）

#### Phase 1-2: 基本メモリキャッシュ
- イベント処理の高速化
- 統計計算結果のキャッシュ

#### Phase 3-4: LRUキャッシュ
- メモリ効率の最適化
- ヒット率測定・チューニング

#### Phase 5-6: 永続化キャッシュ
- 起動時間の大幅短縮
- 大量データ処理の最適化

### テスト戦略（RDD方式）

#### RDDにおけるテストの位置づけ
- **主役は実動作確認**: テストは補助的役割
- **E2Eテスト重視**: 実際の使用シナリオをテスト
- **モック最小化**: 可能な限り実装を使用

#### 各Phaseでの必須確認
1. **実動作確認**: 毎日実行（最重要）
2. **E2Eテスト**: 実使用シナリオ
3. **統合テスト**: 機能追加ごと
4. **パフォーマンステスト**: 週次

## リスク管理

### 技術的リスク
1. **chokidarの制限事項**
   - プラットフォーム依存の挙動
   - 大量ファイルでのパフォーマンス
   - moveイベントの非サポート

2. **スケーラビリティ**
   - 10万ファイル以上での動作
   - リアルタイム性の維持
   - メモリ使用量の増大

3. **データベース管理**
   - SQLiteファイルサイズの増大
   - インデックス設計の重要性
   - バックアップ・復旧戦略

### 対策
- 早期プロトタイプでの検証
- 代替ライブラリの調査（node-watch等）
- 段階的な最適化
- データベース設計の事前検討

## 成功指標

### 定量的指標
- **起動時間**: < 1秒（1万ファイル環境）
- **メモリ使用量**: < 200MB（通常使用時）
- **CPU使用率**: < 5%（アイドル時）
- **イベント検出遅延**: < 100ms
- **データベースサイズ**: < 100MB（1万イベント）

### 定性的指標
- 直感的なUI
- 安定した動作
- 有用な統計情報
- 拡張しやすい設計
- 高いテストカバレッジ

## 詳細計画作成の必須要件

### 各Phase詳細計画書に含めるべき内容
1. **データフロー図**: 入力→処理→出力の明確な流れ
2. **コンポーネント設計**: クラス構造と責務分担
3. **実装手順**: RDD方式での段階的実装
4. **テスト戦略**: 単体・統合・E2Eテストの計画
5. **成功指標**: 機能面・品質面・パフォーマンス面
6. **リスク対策**: 技術的課題と対応策
7. **既存互換性**: 前Phaseとの互換性保証

### データフロー明示の要件
- **入力**: どこから何のデータが来るか
- **処理**: どのコンポーネントでどう変換されるか
- **出力**: どこに何のデータが送られるか
- **永続化**: どのデータがどこに保存されるか
- **エラーフロー**: 異常時のデータの流れ

## RDD実践ガイドライン

### 日次ルーチン
1. **朝**: `npm start`で昨日の作業が動作することを確認
2. **作業中**: 変更のたびに`npm start`で動作確認
3. **夕方**: 本日の変更が統合された状態で動作確認
4. **コミット前**: 必ず実動作確認してからコミット

### 動作しない場合の対応
1. **即座にロールバック**: 動かない変更は議論の余地なく戻す
2. **原因究明**: なぜ動かなかったかを分析
3. **小さく再実装**: より小さな単位で再度実装
4. **動作確認後に進む**: 動くことを確認してから次へ

### RDDの効果測定
- **動作可能日数**: 連続して`npm start`が成功した日数
- **ロールバック率**: 動作しないためロールバックした回数
- **実ユーザーフィードバック**: 実際に使用した人からの評価

## まとめ

このロードマップは、version-02の教訓「テストだけでは品質は保証されない」を踏まえ、**実動作駆動開発（RDD）**を中心とした段階的開発を行う。各Phaseで詳細計画を作成し、データフローを明確に定義することで、常に動作するシステムを維持しながら機能を追加していく。

**RDD最重要原則**: 
- **動かないものは価値がない**
- **毎日`npm start`を実行し、実際に使えることを確認する**
- **テストが通っても動かなければ意味がない**
- **実ユーザーが使えて初めて価値がある**
- **各Phaseは詳細計画を作成すること**
- **データの流れを明示すること**

version-03はこのRDD原則に基づき、確実に動作するcctopを段階的に構築していく。