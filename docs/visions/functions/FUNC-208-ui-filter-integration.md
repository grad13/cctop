# FUNC-208: UI Filter Integration

**作成日**: 2025年7月7日  
**更新日**: 2025年7月7日  
**作成者**: Architect Agent  
**Version**: 1.0.0  
**関連仕様**: FUNC-202, FUNC-203, FUNC-300, FUNC-301  
**実装詳細**: [CG-005: UI Filter Integration Implementation Guide](../supplementary/CG-005-ui-filter-integration-implementation.md)

## 📊 機能概要

3つのフィルター機能（Display Mode、Event Filter、Keyword Filter）の統合仕様。集合論的アプローチによる一貫した動作と、**「unique処理=各ファイルの最新eventを表示する」**定義により処理順序問題を根本解決。

**ユーザー価値**:
- 直感的で予測可能なフィルタリング操作
- 複数フィルタの組み合わせによる柔軟な表示制御
- 削除されたファイルの適切な扱い
- 集合論的操作による理解しやすい動作

## 🎯 機能境界

### ✅ **実行する**
- 3つのフィルター機能の処理順序定義
- フィルタリングロジックの統合
- 集合論的アプローチの実装方針
- 削除されたファイルの適切な表示制御ルール
- unique mode + event filterの正しい処理順序定義

### ❌ **実行しない**
- **フィルタ状態の管理・保持（FUNC-301の責務）**
- **vanilla table管理・操作履歴（FUNC-301の責務）**
- 個別フィルタの実装（FUNC-202, FUNC-203の責務）
- キーボード入力処理（FUNC-300の責務）
- データベース管理（FUNC-000の責務）

## 📋 中核仕様

### **集合論的アプローチの実現**

**基本概念**:
- **Table**: 現在時点の集合を表現
- **操作**: all/unique, event filter, keyword filterにより集合が変化
- **表示**: 最終的な集合がCLI表示される

**更新フロー**（FUNC-301で実装）:
```
1. vanilla tableの集合が更新される
2. 操作履歴をapply
3. 新要素があるなら画面に反映
```

### **FUNC-301との責務分離**

**FUNC-208（本機能）**:
- フィルタリング処理順序の定義
- unique modeの動作仕様
- 削除ファイルの扱いルール

**FUNC-301（状態管理）**:
- FilterState管理
- vanilla table管理
- 操作履歴の保持・適用

### **Unique Mode の確定定義**

**定義**: 「uniqueは最新のを表示する機能」

**正しい処理順序（Pattern: Unique First → Filter Check）**:
1. 各ファイルの最新イベントを特定
2. その最新イベントがevent filterの条件を満たすかチェック  
3. 条件を満たすファイルのみ表示

**具体例**:
```
ファイルA: Create → Modify → Delete (最新)
event filter: Delete除外
結果: ファイルA全体が非表示（CreateもModifyも表示されない）
```

### **3つのフィルター詳細仕様**

#### **1. Display Mode (all/unique)**
**All Mode**:
- 全イベントを時系列で表示
- 同一ファイルの複数イベントがそのまま表示される

**Unique Mode**:
- 各ファイルの最新イベントのみ表示
- GROUP BY file_idで最新のevent_idを取得

#### **2. Event Filter (イベントタイプ絞り込み)**
**対象イベント**:
- Create, Modify, Delete, Move, Find, Restore

**動作**:
- チェックされたイベントタイプのみ表示
- WHERE event_type IN (...) による絞り込み

#### **3. Keyword Filter (ファイル名・パス絞り込み)**
**対象フィールド**:
- file_name (ファイル名)
- directory (ディレクトリパス)

**動作**:
- 部分一致による絞り込み
- WHERE (file_name LIKE '%keyword%' OR directory LIKE '%keyword%')

**実行タイミング**:
- **リアルタイム更新ではない**
- **[Enter]押下時**にDB検索を実行
- 検索結果はvanilla tableの集合に統合

### **データ状態管理**

**状態保持**:
- **メモリ管理**: 操作履歴をメモリで保持
- **操作履歴**: FilterState形式で管理

**更新フロー**:
1. vanilla tableの集合が更新される
2. 操作履歴をapply
3. 新要素があるなら画面に反映

**技術詳細**: [CG-005実装ガイド参照](../supplementary/CG-005-ui-filter-integration-implementation.md)

### **Keyword Filter詳細仕様**

**実行タイミング**:
- **リアルタイム更新ではない**
- **[Enter]押下時**にDB検索を実行
- 検索結果はvanilla tableの集合に統合

**データ管理**:
- DB検索結果をvanilla tableに結合
- 一定件数超過時は古い順に削除

### **動的データ読み込み**

**トリガー条件**:
- 画面内にrowがfillされていない場合
- 選択rowがtable最下部になる場合
- 100msポーリング
- 「end of dataが表示されない」状態での上記条件

**処理**:
- 操作履歴を再度apply
- 結果を画面に反映

### **操作制御・ESC/Enterキー**

**編集モード中の[ESC]**:
- event filter/keyword filter入力モードで[ESC]
- **編集結果を破棄**し、filter設定に入る前の状態を保持してnormal modeに戻る

**編集モード中の[Enter]**:
- event filter/keyword filter入力モードで[Enter]
- **編集結果を保持**し、filter設定を上書きしてnormal modeに戻る

**keyword filter専用の[Shift+Enter]**:
- keyword filter入力モードで[Shift+Enter]
- **DB検索実行**（従来の[Enter]機能）

**Normal Mode中の[ESC]**:
- **全てのeditをclear**
- 初期化状態: **all mode + no filters**

### **適用タイミング**
**基本原則**: リアルタイム更新
**例外**: keyword searchのDB検索のみ[Shift+Enter]時

### **状態の可視化**
**Display Mode**:
- all/unique: **選択中を赤色**などで表示

**Event Filter**:
- 現在の表示内容で確認可能（専用表示不要）

**Keyword Filter**:
- 最上部に表示済み（既存設計）

### **画面更新制御**
**フィルター適用時**:
- DBに新規追加されたeventがfilter条件に合致しない場合
- **画面更新しない**（tableで保持のみ）

### **フィルタ連携の段階的取得**

**動作**: 
- 初回100件取得、最大1000件まで段階的取得
- 表示目標50件を確保するまで繰り返し
- フィルタ条件をSQL条件に含めて効率化

### **確定した仕様定義**

#### **Unique Mode の正式定義**
**「uniqueは最新のを表示する機能」**

#### **正しい処理順序**
**Pattern: Unique First → Filter Check**
1. **Unique処理**: 各ファイルの最新イベントを特定
2. **Filter Check**: その最新イベントがevent filterの条件を満たすかチェック
3. **表示判定**: 条件を満たすファイルのみ表示

#### **具体的な動作例**
**ケース**: ファイルA (Create → Modify → Delete) + Delete除外
- **手順1**: 各ファイルの最新イベント特定 → ファイルAの最新 = Delete
- **手順2**: Delete eventがevent filter対象外かチェック → 対象外
- **結果**: **ファイルA全体が非表示**（CreateもModifyも表示されない）

#### **集合論的アプローチ**
- **Table**: 現在時点の集合を表現
- **操作**: all/unique, event filter, keyword filterにより集合が変化
- **表示**: 最終的な集合がCLI表示される

### **重要な仕様確認事項**

**処理順序の非依存性**:
- event filterとkeyword filterは **AND条件で無矛盾**
- unique処理と組み合わせても **順序非依存**
- 「ファイルごとに最新のeventを表示する」定義により簡略化可能

**実装の自由度**:
- **データ量制限**: 1GB程度のメモリ使用で十分
- **エラーハンドリング**: .cctopディレクトリに適当に出力
- **UI実装詳細**: Runner判断で適切に実装
- **end of data表示**: 「該当データがない場合に表示」で十分

**セッション・パフォーマンス**:
- **セッション永続化**: 概念が不明確のため対応不要
- **処理時間**: 通常は問題にならないレベル
- **100msポーリング**: 今回決定する必要なし

## 🔗 他機能との連携

### **FUNC-202: CLI Display Integration**
- **状態管理**: FilterStateの共有
- **表示更新**: フィルタ結果の描画
- **Uniqueモード**: 本仕様で確定した定義を適用

### **FUNC-203: Event Type Filtering**
- **トグル機能**: イベントタイプの選択/解除
- **状態同期**: フィルタ状態の共有
- **即時反映**: フィルタ変更の即座な適用

### **FUNC-300: Key Input Manager**
- **キーハンドリング**: フィルタ関連キーの処理
- **状態遷移**: フィルタモードの管理
- **ESCキー**: フィルタクリア機能

## 🧪 テスト要件

### **処理順序テスト**
- 削除されたファイルがUnique Modeで適切に除外されることの確認
- フィルタ組み合わせ時の期待動作確認
- エッジケース（削除イベントのみのファイルなど）の動作確認

### **フィルタ組み合わせテスト**
- 3つのフィルタ全てが同時適用された場合の動作確認
- 各フィルタの独立性確認
- ESCキーによるフィルタクリア動作確認

## 💡 使用シナリオ

### **開発中のコード変更追跡**
```bash
# TypeScriptファイルの修正履歴を追跡
1. [/] → "*.ts" → [Enter]  # TypeScriptファイル検索
2. [f] → [m]              # 修正イベントのみ表示
3. [u]                    # 各ファイルの最新修正のみ表示
```

### **設定ファイルの変更監視**
```bash
# 設定ファイル関連の変更を監視
1. [/] → "config" → [Enter]  # 設定ファイル検索
2. [f] → [c][m]             # 作成・修正イベントのみ
3. [a]                      # 全履歴を時系列で表示
```

### **削除されたファイルの確認**
```bash
# 削除されたファイルを確認
1. [f] → [d]    # 削除イベントのみ表示
2. [a]          # All Modeで削除履歴を表示
```

## 🔗 他機能との連携

### **FUNC-202: CLI Display Integration**
- **状態管理**: FilterStateの共有
- **表示更新**: フィルタ結果の描画
- **Dynamic Area**: フィルタ状態の可視化

### **FUNC-203: Event Type Filtering**
- **トグル機能**: イベントタイプの選択/解除
- **状態同期**: フィルタ状態の共有
- **即時反映**: フィルタ変更の即座な適用

### **FUNC-300: Key Input Manager**
- **キーハンドリング**: フィルタ関連キーの処理
- **状態遷移**: フィルタモードの管理
- **ESCキー**: フィルタクリア機能

## 🎯 成功指標

1. **一貫性**: 
   - 3つのフィルタの組み合わせが常に予測可能
   - 削除されたファイルの扱いが一貫している

2. **パフォーマンス**: 
   - フィルタ適用時の即時反映
   - 大量データでも安定した動作

3. **使いやすさ**: 
   - 直感的なフィルタ操作
   - ESCキーでの簡単リセット

4. **信頼性**: 
   - エッジケースでの適切な動作
   - フィルタ状態の正確な管理

## 📝 変更履歴

### v1.0.0 (2025-07-07)
- 初版作成：3つのフィルター機能の統合仕様策定
- 集合論的アプローチによる一貫した動作定義
- Unique Mode確定定義：「各ファイルの最新eventを表示する機能」
- 処理順序問題の根本解決：Pattern: Unique First → Filter Check
- 削除されたファイルの適切な扱い定義
- データ状態管理・動的読み込み・ESCキー挙動の詳細化
- フィルタ連携の段階的取得アルゴリズム策定
- SQL統合クエリの実装指針策定
- 重要な仕様確認事項（処理順序非依存性、実装自由度）定義