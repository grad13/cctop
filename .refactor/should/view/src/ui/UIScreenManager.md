# UIScreenManager.ts

- **行数**: 72行
- **判定**: should
- **理由**:
  1. **Fallback（エラー握りつぶし）**: `destroy()`メソッド（65-70行）でscreen.destroy()のエラーを無条件に握りつぶしており、クリーンアップ失敗の情報が失われる
  2. **責務の混在**:
     - 責務1: ターミナル互換性の問題（Setulc警告）に対処するハック（プロセス.stderr オーバーライド、TERM環境変数設定、blessed内部メソッドのモンキーパッチ）
     - 責務2: blessed screenの初期化・取得・破棄といった標準的なライフサイクル管理

- **推奨アクション**:
  1. ターミナル互換性対策を別クラス（TerminalCompatibilityManager等）に抽出
  2. destroy()のエラーハンドリングを改善：
     - エラーログを記録するか
     - 呼び出し元に結果を通知するか（戻り値で成功/失敗を返す）
     - 条件付きで例外を再スロー
  3. UIScreenManagerは画面オブジェクト自体の管理に専念させる
