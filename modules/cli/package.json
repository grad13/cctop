{
  "name": "@cctop/cli",
  "version": "0.3.0",
  "description": "CLI interface for cctop",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "start": "npm run build && node dist/index.js",
    "ui": "npm run build && node dist/index.js",
    "dev": "npm run build && node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cctop/shared": "^0.3.0",
    "@types/blessed": "^0.1.25",
    "blessed": "^0.1.81",
    "chalk": "^5.4.1",
    "string-width": "^7.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "typescript": "^5.5.3",
    "vitest": "^1.6.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
