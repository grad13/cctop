# UI Managers

This directory contains manager classes that handle specific responsibilities in the UI layer.

## Files

- **EventDataManager.ts** - Manages event storage, retrieval, and statistics
- **InputHandler.ts** - Handles keyboard input and terminal resize events

## Architecture

These managers encapsulate specific domains of functionality, making the main CLIDisplay class more focused and maintainable.